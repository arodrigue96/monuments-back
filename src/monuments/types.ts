export interface MonumentData {
  name: string;
  description: string;
  imageUrl: string;
  country: string;
  city: string;
}

export interface MonumentStructure {
  name: string;
  description: string;
  imageUrl: string;
  country: string;
  city: string;
}
